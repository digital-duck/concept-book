import { defineConfig } from 'vite'

// Change base to '/' when running locally without a sub-path.
// For GitHub Pages deployment: base: '/concept-book/'
export default defineConfig({
  base: '/concept-book/',
})
